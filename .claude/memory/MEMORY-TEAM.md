# Team Memory Index

Shared corrections that have been promoted from developer-private `corrections.jsonl` to team memory via `/publish-memory`. Committed to git, syncs via `git pull`.

Each entry lives in `.claude/memory/team/<category-slug>.md`. Add a new line here when you publish.

| Category | File | Published | Sources |
|----------|------|-----------|---------|
| _(empty — add via `/publish-memory`)_ | | | |

## Seeded memories

These are fundamental cascade rules that pre-date the correction system. They live here so a compiled view shows them on session start.
